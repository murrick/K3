# KANGER 3.5.2.14 — post-shutdown release contract

## Purpose

Stage `3.5.2.14` re-establishes exact release and operations provenance after the qualified Console shutdown lifecycle correction from `3.5.2.13` was integrated into the canonical `3.5.2` shelf.

It introduces no Core, Console, Server, Browser, storage or qualification-runtime change. Its only job is to make the release-shelf contract describe and prove the new integrated state before a fresh VPS package is constructed.

## Anchors

```text
pre-3.5.2.13 release contract shelf:
b0ed1cee70d6a4bbaf3b7690df766b9eae41f891

3.5.2.13 qualified head:
df738ca6657fcc1fa15619e1d2b3cccd4e51b397

3.5.2.13 integrated shelf:
ddbf5ab380b4124013f58bbd655a2131ccba536b

stage branch:
fix/3.5.2.14-post-shutdown-release-contract
```

## Required proof

The release qualifier must prove two independent deltas.

First, the complete delta from the previous qualified release-contract shelf to the post-shutdown shelf is exactly the declared `3.5.2.13` stage:

```text
docs/qualification/3.5.2.13-console-shutdown-lifecycle.md
kanger-console/src/org/kanger/Console.java
kanger-console/src/org/kanger/Kanger.java
kanger-console/src/org/kanger/ShutdownHook.java
kanger-qualification/src/org/kanger/KangerConsoleLifecycleBindingRunner.java
kanger-qualification/src/org/kanger/KangerDiagnosticRunner.java
```

Second, the delta after `ddbf5ab...` is release-contract/documentation material only.

No Server production or Server test file may change in this stage. Server identity remains `server-0.18`.

## Operations consequence

The Server 0.18 package previously built from `b0ed1cee...` is no longer valid deployment provenance for the complete repository candidate. The Server JAR may be byte-identical because `kanger-console/src` is outside the Server Maven production roots, but the operations package must still be regenerated from the newly qualified canonical shelf.

The next operations package must therefore:

- derive from the exact post-`3.5.2.13` release-contract-qualified shelf;
- rebuild and re-hash the Server 0.18 JAR and matched 15-file Browser distribution;
- record exact source and packaging heads;
- preserve the Server 0.14 rollback anchors;
- use a fresh disposable database;
- begin the VPS soak at nested transaction -> level-zero commit -> explicit close -> use/reopen.

## Boundary

This stage authorizes no `release/3.5.2` creation, tag, GitHub Release or production cutover. Its successful qualification only restores eligibility to construct the fresh VPS soak package under the user's explicit decision to proceed toward soak.
