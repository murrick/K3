# 3.7.0 CI retirement and TECH manual follow-up

The active KANGER III 3.7.0 CI surface no longer includes the historical 3.4.1, 3.4.3 and 3.4.4 qualification workflows or the stale architectural Javadoc topology workflow. Their complete definitions and past evidence remain recoverable from Git history.

Retired active workflow files:

- `.github/workflows/kanger-baseline.yml` — KANGER III 3.4.1 semantic planning
- `.github/workflows/kanger-3.4.3.yml` — KANGER III 3.4.3 storage optimization
- `.github/workflows/kanger-3.4.4-dumb-reliability.yml` — KANGER III 3.4.4 DUMB reliability qualification
- `.github/workflows/kanger-javadoc.yml` — stale architectural Javadoc topology

During the upcoming `status` / Browser TECH-panel manual qualification, also verify the two confirmation tails closed immediately before status work:

1. Enter in the Browser confirmation dialog executes the default Confirm action.
2. `erase` with an open/active database explicitly warns that the contents of the currently open database will also be erased.
3. Confirmed `erase` preserves current semantics: storage remains attached/open; this is a UX warning, not a close/detach change.
