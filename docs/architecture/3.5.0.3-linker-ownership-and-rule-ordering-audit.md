# KANGER 3.5.0.3 — Linker ownership and rule-ordering audit

## Scope

This checkpoint audits the ownership and lifecycle boundary of:

```text
kanger/src/org/kanger/Linker.java
```

and resolves one demonstrated execution-order invariant defect without changing the frozen legacy branch-closure kernel.

## Architectural ownership

`Linker` is owned by one `Mind` and is not a persistent graph owner.

```text
Mind
  -> owns one Linker execution component

Linker
  -> observes and mutates query/phase-local Mind state
  -> invokes factories for canonical TValue/FValue/Rule operations
  -> does not serialize itself
  -> does not own storage close/reopen
  -> does not own persistent relation unlink authority
```

The private `solveIndex`, `indexedSolveCounts`, `unarySolveKeys`, and `indexedSolves` fields are acceleration-only state over transient `TSolve` objects already owned by `Mind.ruleSolves`.

Their lifecycle is explicit:

```text
start of link()
  -> mind.ruleSolves.clear()
  -> clearSolveIndex()

workspace/storage clear
  -> Mind replaces Linker instance
```

Therefore the acceleration structures must never become semantic authority and must not survive the query/workspace generation that created their referenced `TSolve` objects.

## Publication boundary

`Linker` does not directly persist a completed rule graph.

Its main publication paths are delegated:

```text
TValue creation/resurrection
  -> TValueFactory

FValue creation/invalidation
  -> FValueFactory

produced Domain materialization
  -> updateDatabase()
  -> Domain.createStored(mind)
  -> RuleFactory / related canonical factories

transient compatibility tuples
  -> Mind.addTSolve(...)
```

`linkDatabase()` remains a deferred-classification and branch-closure kernel. It marks produced state, causes, solves, and temporary hypotheses; `updateDatabase()` performs later materialization. This distinction remains unchanged.

## Symmetry conclusion

No standalone `Linker.link(...) / Linker.unlink(...)` persistent relation pair exists.

The class name describes inference linkage broadly, but graph symmetry is maintained by the underlying owners:

- C-variable parent/child symmetry belongs to `Mind.linkCVar()` / `Mind.unlinkCVar()`;
- canonical TValue lifecycle belongs to `TValueFactory`;
- stored Rule/Domain lifecycle belongs to their factories and transaction protocol;
- transient solve-index cleanup belongs to the Linker/Mind query boundary.

Adding an artificial `unlink` method to `Linker` would encode false ownership.

## Demonstrated rule-ordering defect

`link()` constructs two rule traversal lists from one `Set<IRule>`:

```text
leftList  descending ID order
ruleList  ascending ID order
```

The historical comparators implemented these orders through subtraction followed by narrowing:

```java
return (int) (o2.getId() - o1.getId());
return (int) (o1.getId() - o2.getId());
```

This was not a valid ordering over the full `long` identifier domain.

For IDs separated by `2^32`, a non-zero difference could narrow to zero. Other large distances could reverse sign, and full-range subtraction could overflow before narrowing.

Consequences included:

```text
different Rule IDs compare as equal
ascending/descending traversal may be inconsistent
Comparator contract may be violated
TimSort may receive a non-transitive relation
Linker pass order may depend on overflow rather than Rule ID
```

Ordering is observable because Linker deliberately performs both descending and ascending rotations.

## Invariant

> The first Linker rule traversal is strictly descending by the full `long` Rule ID, and the second traversal is strictly ascending by the full `long` Rule ID.

## Production correction

The production comparators now delegate to package-local regression seams:

```java
static int compareRuleIdsDescending(IRule left, IRule right) {
    return Long.compare(right.getId(), left.getId());
}

static int compareRuleIdsAscending(IRule left, IRule right) {
    return Long.compare(left.getId(), right.getId());
}
```

The active anonymous comparators in `link()` call those exact methods. The methods are package-private, introduce no public API, and exist so the focused regression exercises the production comparison logic rather than a duplicated formula.

Production commit:

```text
1cb054388a53739a5a742008bd701b6627758166
Fix Linker rule ordering over long IDs
```

The diff is limited to:

- two package-private comparison methods;
- delegation from the existing ascending and descending comparators;
- removal of one final blank line.

No rotator order, rule selection, set membership, `isValidFor()`, solve-index behavior, `linkDatabase()` flow, publication timing, persistence format, or canonical identity changed.

## Regression gate

Focused runner:

```text
kanger-console/src/org/kanger/KangerLinkerRuleOrderingSafetyRunner.java
```

It invokes the exact package-local production comparison methods and verifies:

1. ordinary adjacent IDs;
2. the `Integer.MAX_VALUE + 1` boundary;
3. separation by `2^32`;
4. `Long.MIN_VALUE` versus `Long.MAX_VALUE`;
5. comparator antisymmetry;
6. equality behavior;
7. complete ascending sequence order;
8. complete descending sequence order.

Runner commit:

```text
2c3feef21206213fc6c2b42cabc5b9a0c9db2347
Add Linker rule ordering regression gate
```

The runner is invoked explicitly by `.github/workflows/kanger-ci.yml` through:

```text
Linker rule ordering invariant
```

Workflow commit:

```text
6538d2e08890ddf4ee41930ec7bf22f885ea7a5e
Run Linker rule ordering invariant in CI
```

## Final classification

```text
Linker persistent ownership              none
Linker acceleration ownership            query-local
solveIndex semantic authority             no
standalone unlink obligation              no
rule-order comparator defect              resolved
legacy semantic kernel change             none
public API change                         none
focused regression                        present
```

Qualification execution is recorded in the enclosing `3.5.0.3` closure checkpoint.
