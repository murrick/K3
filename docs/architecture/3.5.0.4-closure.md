# 3.5.0.4 — architectural Javadoc stage closure

## Closure parent

```text
7b8d1781835db6b3c8e1b5af2c0599b49bd507c3
Review execution front-end Javadoc checkpoint
```

The review head is qualified by all required workflows:

```text
KANGER architectural Javadoc                success
KANGER CI                                   success
KANGER III 3.4.1 semantic planning          success
KANGER III 3.4.3 storage optimization       success
KANGER III 3.4.4 DUMB reliability           success
```

DUMB succeeded on Ubuntu Java 8, Ubuntu Java 21 and macOS Java 21.

## Stage result

The internal architecture of KANGER is now documented from bootstrap to
physical persistence:

```text
J1   User / Mind
J2   DictionaryFactory / PredicateFactory
J3   DomainFactory / CommentFactory
J4   TVariableFactory / TValueFactory
J5   FunctionFactory / FValueFactory
J6   LibraryFactory
J7   RuleFactory / RuleCandidateIndex
J8   Linker
J9   Escalera / IBase / IData
J10  DUMB DB / Base / Data
J11  Parser / Compiller / Calculator / Analyzer
```

The documentation establishes a coherent ownership chain:

```text
source syntax
  -> AST
  -> canonical semantic units and transactional overlays
  -> fixed-point execution and operation effects
  -> result/hypothesis interpretation
  -> snapshot/materialization boundary
  -> logical persistent bases
  -> physical DUMB generation, WAL, integrity and block store
```

## Demonstrated invariant corrections discovered during 3.5.0.4

Documentation was evidence-first. Production code changed only after focused red
reproduction and permanent regression gating.

```text
J3   Domain waiter rollback
J5   FValue continuation rollback
J7   Rule continuation rollback
J8   Linker local checkpoint balance
J9   storage acquisition atomicity
J10  DUMB close failure isolation
J10  reversible DUMB reindex publication
```

Each correction preserves public signatures, semantic identity, storage formats
and unrelated ordering. Exact evidence is retained in the corresponding
`docs/architecture/3.5.0.4-*` records.

## Permanent protection

The architectural Javadoc workflow now:

- generates implementation documentation on Java 8 and Java 21;
- verifies rendered architecture markers for all owner/orchestrator checkpoints;
- rejects unresolved raw Javadoc links;
- runs the public top-level type coverage inventory;
- uploads rendered Java 21 Javadoc and `javadoc-coverage.tsv`.

The KANGER CI workflow permanently runs all focused lifecycle, ordering,
rollback, storage acquisition, DUMB close and reindex publication invariants.

## Coverage boundary

`3.5.0.4` is complete because every internal owner/orchestrator boundary is
covered. It intentionally does not claim that every public method is already a
usable external API contract.

Assigned to `3.5.0.5 — public API documentation audit`:

- public interfaces and method contracts;
- semantic units, primitives and stores;
- enums and exceptions;
- Leaf/Token and public leaf structures;
- console/bootstrap usability surfaces;
- individual built-in function and predicate operations;
- examples and caller-oriented lifecycle/error guidance.

Excluded from KANGER documentation ownership:

- vendored Mozilla Rhino sources;
- test and diagnostic runners.

## Manual acceptance

The local inspection and execution protocol is:

```text
docs/architecture/3.5.0.4-local-review-runbook.md
```

It includes Maven regression, all permanent invariant runners, rendered Javadoc,
coverage inventory, source-review checks and a disposable-storage smoke/reindex
scenario.

## Stage invariant

The governing invariant remains satisfied:

> `3.5.0` does not intentionally change observable KANGER execution semantics.

The only production changes in `3.5.0.4` are demonstrated lifecycle/atomicity
corrections. J11 itself has a net comment-only Java diff from the qualified J10
closure; one accidental Parser statement introduced during full-file editing was
detected by commit-diff inspection and removed before any qualification.

## Next stage

```text
3.5.0.5 — public API documentation audit
```

PR #31 remains draft until the public API stage is complete and the final
artifact is promoted as:

```text
develop/3.5.0
```
