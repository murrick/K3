# C-variable adjacency diagnostic trigger

This marker exists only to trigger the diagnostic PR workflow whose base branch contains the in-memory Linker variant. No production source is changed by this commit.
