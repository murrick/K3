# KANGER III 3.7.0 — release closure

Date: 2026-08-20

## Product baseline

The final product-bearing baseline before release bookkeeping is:

```text
develop/3.7.0
5430e1faf939e54959aa5986cff290457102d241
```

That head contains the accepted 3.7.0 product semantics plus the isolated visual-test runtime. The test-isolation slice changes Console/test/qualification code only; it does not change Core/User/Mind/storage semantics.

## Qualification already established for the product-bearing head

```text
KANGER CI                         32359928122  PASS
KANGER qualification isolation   32359928145  PASS
DUMB reliability qualification   32359928204  PASS
KANGER architectural Javadoc     32359928137  PASS
```

The historical `KANGER III 3.4.1 semantic planning` and `KANGER III 3.4.3 storage optimization` workflows retain their known legacy-oracle failures and are not interpreted as new 3.7.0 regressions.

## Human acceptance evidence

The 3.7.0 lineage has already received interactive Console and Browser/UI soak. Findings that changed product behavior were corrected and re-qualified. The final delta after that soak is the test-harness isolation correction, whose regression explicitly proves that the working User, current Mind, storage, transaction level and facts survive both isolated test modes unchanged.

Release acceptance for 3.7.0 is therefore based on cumulative human soak plus exact automated qualification, rather than restarting the complete manual soak after a test-only correction.

## Final closure rule

This document, `release-manifest.yaml`, `WHAT'S-NEW-3.7.0.md` and `kanger-server/RELEASE-3.7.0.md` form release bookkeeping only. They add no runtime semantics.

The tip of `releaseprep/3.7.0-closure` must pass current qualification. The server-side release marker deliberately participates in the `KANGER Server` path filter so that Java 8/21 packaging and the Java 21 release artifact are rebuilt from the exact final closure head.

Only after that PASS may the repository advance `develop/3.7.0` to the closure head and create immutable `release/3.7.0` at the identical commit.

## Explicit exclusions

The following are not part of release creation:

- tag creation;
- GitHub Release/publication;
- deployment or production cutover;
- post-3.7.0 `status` observability;
- `.k` transport redesign;
- resource quota/governor research.
