<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.kanger</groupId>
        <artifactId>kanger3-reactor</artifactId>
        <version>3.3-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>kanger-core</artifactId>
    <packaging>jar</packaging>

    <name>KANGER shared semantic core</name>
    <description>
        Shared production compilation boundary consumed by delivery adapters.
        Source files remain in the historical KANGER production roots.
    </description>

    <build>
        <sourceDirectory>${project.basedir}/../kanger/src</sourceDirectory>

        <resources>
            <resource>
                <directory>${project.basedir}/../kanger/resources</directory>
            </resource>
            <resource>
                <directory>${project.basedir}/../kanger-udf/src</directory>
                <includes>
                    <include>**/*.properties</include>
                </includes>
            </resource>
        </resources>

        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>add-shared-production-source-roots</id>
                        <phase>generate-sources</phase>
                        <goals><goal>add-source</goal></goals>
                        <configuration>
                            <sources>
                                <source>${project.basedir}/../kanger-command/src</source>
                                <source>${project.basedir}/../kanger-udf/src</source>
                                <source>${project.basedir}/../kanger-data-dumb/src</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                    <showWarnings>true</showWarnings>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
