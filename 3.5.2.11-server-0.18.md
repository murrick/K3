# KANGER III 3.5.2.11 — Server 0.18 candidate qualification

Status: **MERGED / RELEASE CONTRACT QUALIFIED**  
Date: 2026-08-06

## Repository boundary

```text
corrected release shelf:
develop/3.5.2.9-integration-release-shelf
03310482cebdf55b34829f3d59bdd197edb6275b

candidate branch:
server/3.5.2.11-server-0.18

opening identity head:
b9d3e21d5a664880f7e0d2b7e96c2e33e1e50644

qualified code head:
a16ec7abb9b2df1aebbaed921088184f0e571c47

qualified documentation head:
0213e82023a313641b05ff62d7381da5adc6da09

integration commit:
b967846832586858d42a5e21091154c682948d00

PR #75:
MERGED

candidate identity:
KANGER 3.5.2 / Core 3.3 / API 1 / server-0.18
```

The candidate branch was created exactly from the corrected shelf. PR #75 was
merged with a normal merge commit, preserving the qualified code and
documentation ancestry.

Server 0.17, its operations package and damaged soak database are immutable failed-soak evidence. They are not reusable. The damaged database must not be opened, repaired, reindexed or deleted.

## Candidate delta

The stage changes only the deployable Server identity and its exact assertions:

- generated Server artifact identity `server-0.17` -> `server-0.18`;
- version and HTTP-response identity tests;
- local health/readiness smoke;
- installed-service verifier;
- Java 8/21 Server workflow assertions;
- version contract;
- release manifest;
- lifecycle and qualification documentation.

It introduces no Core semantic change, public API change, Browser runtime
change, storage-format migration or production configuration change.

## Corrected baseline

Server 0.18 contains the integrated `3.5.2.10` explicit storage lifecycle
correction:

- transaction commit never closes physical storage;
- root checkpoint is durable and leaves storage open;
- `use` requires CLOSED and rejects before target validation;
- `close` rejects active transaction levels without implicit commit or rollback;
- ordinary Core `User` owns `use`, `checkpoint` and `close`;
- typed lifecycle failures are shared by Core, Console and Server.

The corrected isolated nested transaction -> commit to level zero -> explicit
close -> reopen path passed automated and manual branch qualification before
integration. Historical causation of the Server 0.17 manifest corruption remains
unproven.

## Identity contract

Every non-empty JSON response reports:

```json
{
  "version": "3.3",
  "core_version": "3.3",
  "api_version": "1",
  "server_version": "server-0.18"
}
```

Generated build metadata reports:

```properties
branch=server-0.18
server.version=server-0.18
source.branch=<exact source branch>
```

`server_version` is the deployable artifact identity. `source.branch` is
provenance only.

## Qualification correction

The opening identity head exposed one stale test-only expectation:

```text
HttpServerTest.everyJsonResponseCarriesCoreApiAndServerIdentity
expected server-0.17, actual server-0.18
```

All other 174 Server tests passed. The assertion was corrected without changing
production code:

```text
a16ec7abb9b2df1aebbaed921088184f0e571c47
Promote HTTP identity assertion to Server 0.18
```

## Exact qualification evidence

The qualified code head and its documentation successor both completed the
applicable matrix successfully:

```text
KANGER Server
KANGER CI
KANGER III 3.4.1 semantic planning
KANGER III 3.4.3 storage optimization
KANGER III 3.4.4 DUMB reliability qualification
KANGER qualification isolation
```

The Server workflow proved:

- Maven `clean verify` on Java 8 and Java 21;
- 175 Server tests with zero failures, errors or skips;
- exact `server-0.18` generated and HTTP identity;
- matched 15-file Browser artifact and containment checks;
- deployable JAR shape and production-source composition;
- deployment asset syntax and loopback topology assertions;
- authenticated session and nested-transaction smoke;
- clean shutdown and active-marker removal;
- Java 21 candidate JAR artifact upload.

Transient GitHub-hosted runner failures occurred before checkout while actions
were unavailable. Only failed jobs were rerun; all repository steps then passed.

## Integration evidence

PR #75 merged the exact documentation head into the corrected shelf:

```text
merge commit: b967846832586858d42a5e21091154c682948d00
merge method: normal merge commit
base:         develop/3.5.2.9-integration-release-shelf
```

The integrated merge triggered the standard Server, Core, storage, DUMB,
Javadoc and Browser matrices. The old specialized release-shelf workflow failed
only because it still encoded the historical Server 0.17 contract. Stage
`3.5.2.12` replaces that stale workflow and qualifier without changing product
or test code.

## Operations boundary

The integrated source is not itself a deployment package. A fresh operations
branch must be derived from the release-contract-qualified shelf and must:

1. build a new Server 0.18 JAR and exact 15-file browser package;
2. record new component and bundle checksums;
3. record canonical source and packaging provenance;
4. create a transactionally quiet pre-deployment snapshot and verified off-host
   copy;
5. use a fresh disposable database;
6. begin the VPS soak at:
   nested transaction -> commit to level zero -> explicit close -> use/reopen;
7. continue broader qualification only after that boundary passes.

No Server 0.18 package checksum or deployment receipt is claimed by this source
stage.

## Release boundary

```text
Server 0.18 source integration: PASS
release contract:              QUALIFIED by stage 3.5.2.12
release/3.5.2:                 not created
immutable tag:                 not created
GitHub Release:                not created
VPS deployment:                not yet performed
production:                    release/3.5.1 / server-0.14
```

The user's explicit decision authorizes merge, fresh package construction and
VPS soak deployment. Immutable release fixation and permanent production
acceptance remain separately evidenced transitions.
