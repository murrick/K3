# KANGER III 3.5.2.11 — Server 0.18 candidate qualification

Status: **IDENTITY QUALIFIED / DOCUMENTED IN BRANCH / DRAFT / UNMERGED**  
Date: 2026-08-06

## Repository boundary

```text
corrected release shelf:
develop/3.5.2.9-integration-release-shelf
03310482cebdf55b34829f3d59bdd197edb6275b

working branch:
server/3.5.2.11-server-0.18

opening identity head:
b9d3e21d5a664880f7e0d2b7e96c2e33e1e50644

qualified code head:
a16ec7abb9b2df1aebbaed921088184f0e571c47

Draft PR:
#75 — OPEN / DRAFT / UNMERGED

candidate identity:
KANGER 3.5.2 / Core 3.3 / API 1 / server-0.18
```

The working branch starts exactly from the corrected shelf merge commit. The predecessor Server 0.17 candidate, operations package and damaged soak database remain immutable failed-soak evidence and are not reusable.

## Candidate delta

The stage changes only the deployable Server identity and its exact qualification assertions:

- generated Server artifact identity `server-0.17` → `server-0.18`;
- version unit and HTTP-response identity tests;
- local health/readiness smoke;
- installed-service verifier;
- Java 8/21 Server workflow assertions;
- version contract;
- release manifest;
- lifecycle and qualification documentation.

It introduces no Core semantic change, public API change, Browser runtime change, storage-format migration or production configuration change.

## Corrected baseline

Server 0.18 contains the integrated `3.5.2.10` explicit storage lifecycle correction:

- transaction commit never closes physical storage;
- root checkpoint is durable and leaves storage open;
- `use` requires CLOSED and rejects before target validation;
- `close` rejects active transactions without implicit commit or rollback;
- ordinary Core `User` owns `use`, `checkpoint` and `close`;
- typed lifecycle failures are shared by Core, Console and Server.

The corrected isolated nested-commit → level-zero → close → reopen scenario passed automated and manual branch qualification before integration. Historical causation of the Server 0.17 manifest corruption remains unproven.

## Identity contract

Every non-empty JSON response reports:

```json
{
  "version": "3.3",
  "core_version": "3.3",
  "api_version": "1",
  "server_version": "server-0.18"
}
```

Generated build metadata reports:

```properties
branch=server-0.18
server.version=server-0.18
source.branch=<exact source branch>
```

`server_version` is the deployable artifact identity. `source.branch` is provenance only.

## Qualification correction

The opening identity head `b9d3e21d...` exposed one stale test-only expectation:

```text
HttpServerTest.everyJsonResponseCarriesCoreApiAndServerIdentity
expected server-0.17, actual server-0.18
```

All other 174 Server tests passed in that run. The assertion was corrected without changing production code:

```text
a16ec7abb9b2df1aebbaed921088184f0e571c47
Promote HTTP identity assertion to Server 0.18
```

## Exact qualified-code-head matrix

All workflows automatically applicable to this candidate delta completed successfully on `a16ec7abb9b2df1aebbaed921088184f0e571c47`:

```text
31114409512 — KANGER Server — success
31114409102 — KANGER CI — success
31114409161 — KANGER III 3.4.1 semantic planning — success
31114410030 — KANGER III 3.4.3 storage optimization — success
31114409206 — KANGER III 3.4.4 DUMB reliability qualification — success
31114409013 — KANGER qualification isolation — success
```

The storage and isolation workflows initially encountered GitHub-hosted runner infrastructure failures before checkout:

```text
Service Unavailable / Bad Gateway
Failed to resolve action download info
```

Only failed jobs were rerun. Their repository steps then completed successfully. These transient platform failures are not candidate failures.

The Server workflow proves:

- Maven `clean verify` on Java 8 and Java 21;
- 175 Server tests with zero failures, errors or skips;
- exact `server-0.18` generated and HTTP identity;
- matched 15-file Browser artifact and containment checks;
- deployable JAR shape and production-source composition;
- deployment asset syntax and loopback topology assertions;
- authenticated session and nested transaction smoke;
- clean shutdown and active-marker removal;
- Java 21 candidate JAR artifact upload.

The independent workflows additionally prove semantic, storage, DUMB integrity/recovery and qualification-isolation invariants across their configured Java and operating-system matrices.

The architectural Javadoc workflow was not triggered by this identity/test/documentation-only delta. No production Javadoc source changed; its qualified corrected-shelf state is inherited unchanged rather than represented as a new exact-head run.

## Qualification interpretation

The Server 0.18 source identity is now automatically qualified in branch. This does not constitute:

- release-shelf integration;
- release acceptance;
- operations-package qualification;
- VPS soak evidence;
- production deployment authority.

No Server 0.18 JAR checksum, matched browser-package checksum or deployment receipt is fixed by this source qualification document.

## Operations boundary

After a separate authorization:

1. integrate the qualified candidate state according to the repository lifecycle;
2. create a fresh operations branch/package from the accepted Server 0.18 candidate head;
3. build a new JAR and matched browser package;
4. record new checksums and provenance;
5. use a fresh disposable database;
6. resume the VPS soak at the former failure boundary:
   nested transaction → commit to level zero → explicit close → use/reopen;
7. continue the full soak only after that boundary passes.

The frozen Server 0.17 damaged database must not be opened, repaired, reindexed or deleted.

## Release boundary

This branch and Draft PR authorize none of the following:

```text
merge to the release shelf
release/3.5.2 creation
tag or GitHub Release publication
VPS deployment
production cutover
```

Production remains on accepted `release/3.5.1 / server-0.14`.
