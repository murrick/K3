# KANGER III 3.5.2.11 — Server 0.18 candidate opening

Status: **IDENTITY ESTABLISHED / QUALIFICATION NOT PERFORMED / UNMERGED**  
Date: 2026-08-06

## Repository boundary

```text
corrected release shelf:
develop/3.5.2.9-integration-release-shelf
03310482cebdf55b34829f3d59bdd197edb6275b

working branch:
server/3.5.2.11-server-0.18

candidate identity:
KANGER 3.5.2 / Core 3.3 / API 1 / server-0.18
```

The working branch was created exactly from the corrected shelf merge commit. The predecessor Server 0.17 candidate and its operations package remain immutable failed-soak evidence and are not reusable.

## Purpose of this opening commit

This commit establishes the next deployable server identity and records its provenance. It changes only:

- the generated Server artifact identity from `server-0.17` to `server-0.18`;
- exact unit, smoke, installed-service and CI identity assertions;
- `release-manifest.yaml`;
- lifecycle and candidate-opening documentation.

It introduces no Core semantic change, public API change, Browser runtime change, storage-format migration or production configuration change.

## Corrected baseline

Server 0.18 includes the integrated `3.5.2.10` explicit storage lifecycle correction:

- transaction commit never closes physical storage;
- root checkpoint is durable and leaves storage open;
- `use` requires CLOSED and rejects before target validation;
- `close` rejects active transactions without implicit commit or rollback;
- the ordinary Core `User` owns `use`, `checkpoint` and `close`;
- typed lifecycle failures are shared by Core, Console and Server.

The corrected isolated nested-commit → level-zero → close → reopen scenario passed automated and manual branch qualification before integration. Historical causation of the Server 0.17 damaged manifest remains unproven.

## Identity contract

Every non-empty JSON response must report:

```json
{
  "version": "3.3",
  "core_version": "3.3",
  "api_version": "1",
  "server_version": "server-0.18"
}
```

The generated build metadata must report:

```properties
branch=server-0.18
server.version=server-0.18
source.branch=<exact source branch>
```

`server_version` is the deployable artifact identity. `source.branch` is provenance only.

## Qualification boundary

This opening commit is not qualification evidence. The next stage must prove on its exact head:

- Maven `clean verify` on Java 8 and Java 21;
- all standard Core, semantic, storage, DUMB, isolation, Javadoc, Server and Browser workflows;
- exact `server-0.18` JAR identity;
- no unrelated Core/API/Browser product delta from `03310482...`;
- local health, readiness, authentication, nested lifecycle and clean-shutdown smoke;
- reproducible artifact and browser inventory.

A Draft PR may be opened only as an unmerged qualification surface. Passing CI does not authorize integration, packaging, deployment or release acceptance.

## Operations boundary

No Server 0.18 operations package, JAR checksum, browser package checksum or deployment receipt exists at this checkpoint.

After exact-head automated qualification and a separate integration decision:

1. create a fresh operations branch from the accepted Server 0.18 candidate head;
2. build a new JAR and matched browser package;
3. record new checksums and provenance;
4. use a fresh disposable database;
5. resume the VPS soak at the former failure boundary:
   nested transaction → commit to level zero → explicit close → use/reopen;
6. continue the full soak only after that boundary passes.

The frozen Server 0.17 damaged database must not be opened, repaired, reindexed or deleted.

## Release boundary

This commit authorizes none of the following:

```text
merge to the release shelf
release/3.5.2 creation
tag or GitHub Release publication
VPS deployment
production cutover
```

Production remains on accepted `release/3.5.1 / server-0.14`.
