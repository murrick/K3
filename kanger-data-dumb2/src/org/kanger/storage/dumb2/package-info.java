/**
 * DUMB 2.0 semantic storage implementation boundary.
 *
 * <p>The stable DUMB backend remains in {@code kanger-data-dumb}. This package
 * is intentionally isolated so DUMB 2.0 can evolve its Context identity,
 * revision and multi-context semantics without changing the 3.7.0 reference
 * backend or inheriting its physical-format compatibility constraints.</p>
 */
package org.kanger.storage.dumb2;
